{
  "title" : "Starter KIT",
  "html" : "<pre># angular starter kit # author: Marcin Sołtysiuk # version 1.0 # description: angular frontend development environment with sass # requirements: nodejs, phantomjs, npm, bower, sass</pre> <h3>1. Install</h3> <p>---------------------------------------</p> <p>First please install nodejs, phantomjs, npm, bower, sass then run cmd 'install.sh'</p><h3>2. Run</h3> <p>---------------------------------------</p> <p>How to run frontend env on localhost:9000 ? run from cmd 'grunt server'</p><h3>3. Build production app</h3> <p>---------------------------------------</p> <p>run from cmd 'grunt build'app is available from folder /target/app</p> <h3>4. Unit tests</h3> <p>---------------------------------------</p> <p>run unit tests from cmd 'frontend-test.sh'run coverage from cmd 'frontend-coverage.sh'then we can open graph from coverage\\PhantomJS 1.9.1 (Windows 7 0.0.0)\\index.html</p> <h3>5. Developer environment:</h3> <p>---------------------------------------<p> <p>open with sublime text editor file syngular.sublime-project</p>"}